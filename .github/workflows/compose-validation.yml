name: Compose Validation

on:
  push:
    paths:
      - 'docker-compose*.yml'
      - 'Dockerfile'
      - '.env.example'
  pull_request:
    paths:
      - 'docker-compose*.yml'
      - 'Dockerfile'
      - '.env.example'

jobs:
  validate-compose:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compose-file:
          - docker-compose.yml
          - docker-compose.staging.yml
          - docker-compose.production.yml
    steps:
      - uses: actions/checkout@v4

      - name: Validate ${{ matrix.compose-file }}
        run: |
          if [ -f "${{ matrix.compose-file }}" ]; then
            docker compose -f ${{ matrix.compose-file }} config --quiet
            echo "${{ matrix.compose-file }} is valid"
          else
            echo "${{ matrix.compose-file }} not found, skipping"
          fi

      - name: Check for required services
        if: hashFiles(matrix.compose-file) != ''
        run: |
          SERVICES=$(docker compose -f ${{ matrix.compose-file }} config --services 2>/dev/null || true)
          echo "Services found: $SERVICES"
